# Stroom-ingestie. Draait in de GitHub Action; Python->Neon direct + grids->S3/R2.
requests>=2.32
numpy>=1.26
xarray>=2024.6
netCDF4>=1.6
psycopg[binary]>=3.2
pyarrow>=16.0            # grids als parquet (zstd + byte-stream-split)
boto3>=1.34             # S3-compatibele objectopslag (R2)
